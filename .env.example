# LEI 看盘系统 · 环境变量示例
# 复制为 .env 并填入真值。.env 已被 .gitignore 忽略，绝不提交真实凭据。

# ---------------- 存储 ----------------
# SQLite 路径（默认项目根 lab.db）
# LEI_SQLITE_PATH=/Users/you/lei-signal-lab/lab.db

# ---------------- ark（表达层 LLM，仅用于把 alert 讲成人话）----------------
# 缺 ARK_API_KEY 时监督员自动降级为模板渲染，功能不中断。
# ARK_API_KEY=
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# ARK_MODEL=ark-code-latest

# ---------------- 飞书自定义机器人 Webhook ----------------
# 在飞书群中添加「自定义机器人」后复制 Webhook 地址；只用于出站推送。
# FEISHU_WEBHOOK_URL=
# 回执页公网 HTTPS 根地址。不要写死域名；可填 Cloudflare Tunnel 地址。
# 例如：https://lei-actions.example.com
# FEISHU_ACTION_BASE_URL=
# HMAC-SHA256 签名密钥。请使用随机长字符串，不能与 Webhook 地址相同。
# FEISHU_ACTION_SECRET=
# 可选：回执服务未配置密钥或公网地址时，不会生成可写动作链接。
# Cloudflare Tunnel 示例（在项目根目录执行）：
#   cloudflared tunnel --url http://127.0.0.1:8000
# 然后将输出的 https://*.trycloudflare.com 填入 FEISHU_ACTION_BASE_URL。

# Agent 超级入口表达层 LLM（GLM，OpenAI 兼容）。优先级 GLM > DeepSeek > ARK。
# GLM_API_KEY=（真实密钥只写在 .env，绝不提交）
# GLM_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4  # Coding Plan 专用网关
# GLM_MODEL=glm-5.3
