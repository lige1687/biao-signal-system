# Plan: 真实全A + 全标普宽度面板，可收起紧凑顶栏

## 问题
1. A 股面板用的是 CSI_300（300 只），用户要**真实全 A**（5889 只）
2. 顶栏偏宽（2×280px panel），不能收起
3. 分位信息看不到（首次无历史）

## 方案

### 1. 全 A 真实入库 `scripts/round5_repro/ingest_all_a.py`
- Eastmoney `push2/clist/get` 拉全 A 列表（已验证 5889 只，f13=1→.SS, f13=0→.SZ）
- Tencent 前复权日线，20 线程并发（0.21s/只 → ~1.5min）
- 中证全A 指数 `sh000985`（已验证 251 根日线）作为 CN_ALL_A 指数
- 写 `CN_ALL_A.parquet` universe + index + 5889 个 component bars
- 更新 manifest

### 2. 修复 backfill 脚本
- 用 component bars 的日期轴（不是 index 日期轴）做回算
- 按时间顺序遍历每个 session → 算 breadth → 写 SQLite → 用已累积历史算 percentile → UPDATE 回去
- min_periods=60（250 根日线可覆盖 ~190 个有效分位）

### 3. 后端 service 改动 `market_context_service.py`
- `_DEFAULT_GLOBAL_MARKETS` 改回 `(CN_ALL_A, SP500)`
- strip 快路径：先读 SQLite 最新 revision（<100ms），只在缓存过期时重算
- `_global_panel_symbol`: CN_ALL_A→`000001.SS`, SP500→`^GSPC`
- 显示名：`全A` / `标普500`

### 4. 前端：可收起 + 紧凑 strip `MarketBreadthStrip.tsx`
- **收起态**（默认）：单行细条 ~28px 高，`全A 68%↑ | 标普 53%↓  [▾]`
- **展开态**：~70px 高，2 个紧凑面板（~160px each），显示 B20/B50/分位/5日变化
- 收起状态存 `localStorage("ws.breadthCollapsed")`
- 分位有值时显示 `P85`，无值时省略（不显示 `--`，减少视觉噪音）

### 5. Gates
pytest / ruff / mypy / npm build / git diff --check
