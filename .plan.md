# Plan: 提交后 agent 符合性核对(硬阻断 + 抽屉协商)+ 表单标签 + ATR 接近预警

## 背景
当前建计划:`CreatePlanDialog` 提交 = `createPlan` + `confirmPlan` 一步到位,`confirm` 端点零核对(`store.confirm_plan` 只查必填字段)。用户要:提交后 agent 按 LEI 体系核对入场/退出逻辑,**硬规则不达标不让确认**,抽屉里协商改完再确认。顺带两个已确认的小事:abcd/long-short 看不懂(改中文,卖出计划隐藏 module);止盈止损精确价触发(改 ATR 接近预警,保留击穿确认)。

已定决策(本轮对话):
- LL/AL/底部结构是**确认信号**,不补 E/F 模块。A/B/C/D 仍是规格 §9 的四个进场模块。
- 持仓卖出计划(holding_watch)**不需要 module**(monitor 已对 holding_watch 跳过模块检查)。
- 核对力度:**硬阻断**(硬规则不达标不能确认,软规则只提醒)。
- 协商方式:**抽屉对话**(复用 AgentDrawer + chat 端点)。

---

## Phase 1: 草稿符合性核对 + 硬阻断 + 抽屉协商(主功能)

### 1.1 后端:草稿符合性判定 `src/lei_signal/plans/conformance.py`(新文件)
新函数 `evaluate_draft_conformance(plan, ctx) -> ConformanceReport`,按 `plan_kind` 分支。**无新判定规则**,只组合既有 DTO 字段 + §13/§14 原则 + `module_of`。

**entry 计划(入场逻辑核对):**
- 硬阻断(`can_confirm=False`):
  - `MODULE_NOT_IMPLEMENTED`:module 不在 IMPLEMENTED_MODULES(无法监督)
  - `ENTRY_BLOCKED_BY_TRADABILITY`:§13 可交易性阻断
  - `MISSING_INVALIDATION`:§13.3 失效价未填
  - `INVALIDATION_ALREADY_BREACHED`:§14 失效价已被当日收盘击穿(不该再开仓)
  - `MODULE_RULE_MISMATCH`:`plan.module != module_of(plan.entry_rule_id)`(entry_rule_id 存在时,结构自相矛盾)
  - `DIRECTION_MISMATCH`:plan.direction 与匹配机会方向不一致(红线4,匹配机会存在时)
- 软建议:
  - `RR_BELOW_IDEAL`:R-R<3(2–3 可接受,用户 2026-08-04 定只提示)
  - `RR_NOT_COMPUTABLE`
  - `MODULE_SCENARIO_MISMATCH`:plan.module 与系统检测到的主力场景模块不一致(用户可能有理,只提醒)
  - `PLAN_ORPHANED`:entry_lifecycle_id 当日不在机会里

**holding_watch 计划(退出逻辑核对):**
- 硬阻断:
  - `TP_SL_DIRECTION_INSANE`:long 时 `TP<=close` 或 `stop>=close`(止盈在下/止损在上,方向反了);short 反向
  - `MISSING_EXIT_TRIGGER`:止盈价/止损价/信号全空(`confirm_holding_watch` 已校验,提前暴露)
- 软建议:退出信号 rule_id 当日未命中(可选)

返回 `ConformanceReport(can_confirm: bool, hard_issues: list[Issue], soft_issues: list[Issue], system_detected: {module, direction, scenarios})`。Issue = `{code, severity, rule_id, evidence, next_step_cn, principle_source?}`(复用 PlanAlert 形态)。

### 1.2 后端:端点改动 `api/routes/plans.py` + `schemas.py`
- 新 `GET /api/plans/{plan_id}/conformance` -> `ConformanceReportDTO`。取 plan + `service.get(symbol)` + `context_from_result` -> `evaluate_draft_conformance`。
- 改 `POST /api/plans/{plan_id}/confirm`:**统一入口**。先跑 `evaluate_draft_conformance`,有硬项 -> 422 + 报告;无硬项 -> 按 `plan_kind` 调 `confirm_plan` 或 `confirm_holding_watch`。
- 改 `POST /api/plans/holding-watch`:**只建 draft**(去掉内嵌的 `confirm_holding_watch`),确认统一走 `/confirm`。
- 新 `PUT /api/plans/{plan_id}/draft`:编辑 draft 字段(UPDATE trade_plans 行 + 重写 revision_no=0 快照)。仅 `state=draft` 可改,不走 drift(草稿无既定失效价可漂移)。非 draft -> 409。

### 1.3 后端:ATR 接入 MonitorContext(Phase 3 复用,顺手做)
- `MonitorContext` 加 `atr: float | None = None`。
- `context_from_result` / `from_symbol_detail`:从 `frame["atr14"]` / chart 透传(不新算)。

### 1.4 前端:ReviewDrawer(基于 AgentDrawer 扩展) `web/src/components/ReviewDrawer.tsx`(新)
- `CreatePlanDialog` 提交改为:`createPlan`(不 confirm)-> 关表单 -> 开 `ReviewDrawer(planId)`。
- ReviewDrawer 打开即 `GET /conformance`:硬项红(阻断,带 §出处)/软项黄(建议)/系统检测对比(module/direction)。
- 「确认生效」按钮:`can_confirm` 才可点 -> `confirmPlan`(422 回显硬项);成功 -> invalidate plans -> 关闭。
- 「编辑计划」按钮:重开 `CreatePlanDialog`(edit 模式,预填 draft)-> `PUT /draft` -> 重新 `GET /conformance`。
- 底部 chat:复用 `planChat` 问 agent(接地/降级标记沿用)。
- holding_watch:`createHoldingWatch` 改为建 draft -> 开 ReviewDrawer(退出逻辑核对)-> `/confirm`。

### 1.5 前端:CreatePlanDialog edit 模式
- 加 `editPlanId?: string` prop:set 时提交走 `PUT /draft`,预填从现有 draft 取。
- holding_watch 模式:隐藏 module 字段(见 Phase 2)。

### 1.6 测试 `tests/unit/`
- `test_conformance.py`:entry 六类硬项各自触发 + holding_watch 方向错;`can_confirm` 真值;软项分类;ATR/ctx 缺失不崩。
- `test_plans_routes.py` 扩展:`/confirm` 硬项 422 + 无硬项通过;`/draft` PUT 仅 draft 可改(409);`/conformance` 返回结构。
- mock ark 不打真实 API(沿用 test_plans_llm 范式)。

---

## Phase 2: 表单标签中文 + 卖出计划隐藏 module(小)
- 前端共享映射 `web/src/modules.ts`(新):`MODULE_CN = {A:"A·上升趋势回调", B:"B·密集区突破", C:"C·2B/破底翻", D:"D·假突破反向"}`,`DIRECTION_CN = {long:"做多", short:"做空"}`。
- `CreatePlanDialog`:module 下拉显示中文名;direction 显示做多/做空;**holding_watch 模式隐藏 module**(存 `""` 或占位,UI 不渲染)。
- `SupervisorPage` / `BuyPointDrawer` / `ReviewDrawer`:展示中文(module/direction 经映射)。
- 后端 `llm.py` 模板「模块{module}」内部可保留字母码;用户可见处一律中文。

---

## Phase 3: ATR 接近预警(监控侧)
- `monitor.py`:新 alert code `TAKE_PROFIT_APPROACHING` / `STOP_PRICE_APPROACHING`。
- `_holding_watch_alerts` 加:`|close - preset| <= proximity_atr * atr` 且未击穿 -> `SEVERITY_REMIND`(接近预警)。保留原 `TAKE_PROFIT_REACHED` / `STOP_PRICE_BREACHED`(击穿确认)。
- 阈值走 `get_rule("holding_watch_guard").param("proximity_atr", 1.0)`,源码无裸数值。
- ATR 缺失(atr is None):跳过接近预警,不影响击穿判定。
- 测试:接近未击穿报 APPROACHING / 已击穿只报 REACHED+BREACHED 不重复 / ATR 缺失不报接近。

---

## Gates
`pytest -q` 全绿 / `ruff check src` / `mypy src` / `cd web && npm run build`(tsc 通过)。
红线:chat 输出含「买入」必降级;conformance 不算 DTO 之外的新数值;无数量/金额/账户字段。

## 交付顺序
1. Phase 1.1 + 1.2 + 1.6(后端核对 + 端点 + 测试)-- 核心,前端依赖。
2. Phase 1.4 + 1.5(前端 ReviewDrawer + edit 模式)-- 手验:建 draft -> 核对 -> 协商改 -> 确认。
3. Phase 2(标签中文 + 隐藏 module)-- 快。
4. Phase 3(ATR 接近预警)-- 监控侧独立。

## 不做(本期)
- 不新增 E/F 模块(LL/AL/底部结构为确认信号,已确认)。
- chat 不做流式。
- 不做计划 drift 的 Web UI(继续走 skill `plan_cli revise`)。
