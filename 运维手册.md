# lei-signal-lab 运维手册（看板自启 / 重启 / 排障）

> 适用：本机 macOS，看板 = 后端(127.0.0.1:8000) + 前端(http://localhost:5173) + 全A宽度预计算(交易日16:30)。
> 所有进程由 **macOS launchd** 统一管理，登录自起、崩溃自愈、重启电脑自动恢复。

---

## 一句话结论

**自启装好之后，日常你啥都不用干。** 改了代码想生效，直接打一句 `biao` 就行（等同 `biao restart`）。
不要手动 `uvicorn` / `vite` / `start_*.sh`——会和 launchd 抢端口。

---

## 0. 先让 `biao` 命令生效（仅首次）

`biao` 是我加到你 `~/.zshrc` 的别名。新开一个终端窗口，或当前窗口执行：

```bash
source ~/.zshrc
```

之后直接打 `biao` 即可。不想要了就删掉 `.zshrc` 里那行 `alias biao=...`。

---

## 1. 改了代码，想让改动生效 → 重启

```bash
biao                  # 直接打 = 干净重启 后端+前端（★最常用的一个★）
biao restart         # 同上，显式写
biao restart-backend # 只重启后端
biao restart-frontend# 只重启前端
```

原理：让 launchd 自己先把旧进程杀掉再拉起（`launchctl kickstart -k`），不会和别人抢端口。
改完 Python 后端 / React 前端代码后，跑这一句就生效，不用关终端、不用重启电脑。

---

## 2. 首次安装 / plist 改过之后 → 重装自启

```bash
biao install
# 等价于： bash ~/Desktop/lei-signal-lab/scripts/install_app_autostart.sh
```

会做三件事：
1. 清掉 8000/5173 上的手动旧进程（防抢端口）；
2. 把 `scripts/com.lei.*.plist` 复制到 `~/Library/LaunchAgents/`；
3. `launchctl bootstrap` 加载三个 agent（后端 / 前端 / 宽度预计算），并 `RunAtLoad` 立即启动。

**只在以下情况需要跑：**
- 换了电脑 / 重装系统后；
- 我改了 `scripts/com.lei.*.plist` 之后（比如本次修了崩溃循环）；
- `biao status` 显示三个 agent 没被 load。

---

## 3. 临时停用 / 重新启用

```bash
biao stop     # 停用三个 agent（看板暂时关掉）
biao start    # 重新启用三个 agent
```

> 注意：`stop` 只是 `bootout`，不会删文件；下次 `biao start` 或重启电脑都能恢复。

---

## 4. 验证服务是不是活着

```bash
biao status
```

输出里看两行：
- `launchctl list` 应出现 `com.lei.backend` / `com.lei.frontend` / `com.lei.ashare.ma`；
- `:8000 -> HTTP 200` 且 `:5173 -> HTTP 200` 即正常。

或者直接：
```bash
curl -s -m5 -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/   # 应返回 200
```

---

## 5. 排障

### 5.1 页面报 "Failed to fetch" / 看不到信息
1. 先 `biao status` 看 8000 是不是 200；
2. 若是 000 → 后端没起来，跑 `biao`（或 `biao restart`）；
3. 仍不行 → `biao install` 重装自启，再 `biao`。

### 5.2 曾经踩过的坑：后端/前端崩溃循环（Operation not permitted）
- **症状**：`logs/backend.err.log` 刷屏 `/bin/bash: .../start_backend.sh: Operation not permitted`，端口始终起不来。
- **根因**：旧 plist 用 `/bin/bash` 执行带 `com.apple.provenance` 属性的 `.sh` 脚本，macOS 在 launchd 受限上下文里拒绝执行。
- **已修复**：backend/frontend plist 改为**直接 exec 二进制**（venv python 跑 uvicorn / managed node 跑 vite），不走 bash 包装。若你是从更早版本升级，跑一次 `biao install` 即可载入修好的 plist。

---

## 6. ⚠️ 千万别做的事

| 不要做 | 为什么 |
|---|---|
| 手动 `uvicorn ...` / `npm run dev` / `bash start_backend.sh` | launchd 已经占了 8000/5173，你再起一份会**端口冲突**、服务反而挂掉 |
| 用旧的自制 restart 命令 | 同上，会起第二个进程抢端口 |
| 直接 `kill` 掉后端进程后不重启 | launchd 的 `KeepAlive` 会立刻把它拉起来（这是正常自愈，不是卡死） |

**正确做法永远是：改代码 → `biao`；服务异常 → `biao status` → 按需 `biao` / `biao install`。**

---

## 7. 命令速查表

| 你想做 | 命令 |
|---|---|
| 改完代码让改动生效 | `biao`（或 `biao restart`） |
| 只看后端/前端 | `biao restart-backend` / `biao restart-frontend` |
| 看三个进程+端口状态 | `biao status` |
| 首次/重装自启 | `biao install` |
| 临时关看板 | `biao stop` |
| 重新开看板 | `biao start` |

---

## 8. 技术背景（给想知道的人）

- **launchd 优于手动**：`RunAtLoad=true` + `KeepAlive=true` = 登录自起、崩溃自愈、重启电脑自动恢复，从此告别手动 restart。
- **为什么 plist 直接 exec 二进制**：macOS 沙箱会拒绝 launchd 执行带 `com.apple.provenance` 等属性的 shell 脚本；直接 exec `python3 -m uvicorn` / `node vite` 则无此限制（与项目中 `com.lei.ashare.ma` 等 python agent 同款写法）。
- **三个 agent**：`com.lei.backend`(8000) / `com.lei.frontend`(5173) / `com.lei.ashare.ma`(交易日16:30 跑宽度预计算)。
- **管理脚本**：`scripts/biao-ctl.sh`（本手册所有 `biao` 命令的底层实现；缺省动作 = restart）。

---

## 9. 行业板块趋势预计算（/sectors 数据源）

`/sectors` 行业板块趋势工作台依赖一份「冻结快照」：`sector_trend_snapshot.json` / `sector_trend_history.json` / `sector_members.json`（落在 `LEI_CACHE_ROOT`）。它由**第四个 agent** `com.lei.sector.trend` 在每个交易日 **16:45**（错开 16:30 的全A宽度/日K 维护）自动跑：

```bash
python scripts/precompute_sector_trend.py
```

### 9.1 任务链（顺序很重要）
1. **16:30** `com.lei.ashare.ma`：维护全A日K缓存 `a_share_klines.parquet`（含科创板）。这是 16:45 板块趋势的**前置依赖**——没有它，板块等权指数算不出来。
2. **16:45** `com.lei.sector.trend`：读 `a_share_klines.parquet` + 拉东财板块成分股 → 合成等权指数 / RS / 宽度 / 阶段 → 原子写三份 json。

> 手动调试可加 `--limit 20`（只算前 N 板块）、`--no-save`（不落盘）、`--force`（强制重算忽略当日缓存）。

### 9.2 故障排查
| 现象 | 处理 |
|---|---|
| `/sectors` 打开提示「尚未运行预计算」 | 快照缺失/过期。先确认 16:30 任务已跑出 `a_share_klines.parquet`，再 `biao start` 或手动跑 `precompute_sector_trend.py` |
| 成分股拉取失败（东财 clist 超时） | 跑 `python scripts/precompute_sector_trend.py --force` 重试；部分板块失败不影响其余，失败项进快照 `warnings` |
| 快照 `date` 停更在某天 | 看 `scripts/logs/precompute_sector_trend.err.log`；多半是 16:30 的 K线缓存没更新，回到 9.1 排查 |
| 首次运行 RS 排名变化/百分位为 null | 属正常：首跑无历史可比对，`rs_pctile_delta_20` 显示「留痕中」，后续交易日自动填充 |

### 9.3 判定口径说明（给用户）
板块等权指数为**本机合成**（非行情软件板块指数）；成分股以**当前成分回溯**（含前视偏差，仅形态参考）；**不含北交所**（腾讯无历史）；**MA200 留痕中**（320 日窗口内仅约 121 天）。以上口径在 `/sectors` 页顶部「口径声明条」常驻展示，所有判定标 `research_proxy`，不冒充 LEI 原始规则、不出买卖点。

---

## 10. 美国宏观（/fundamentals「美国宏观」区，FRED 数据源）

对齐 themarketmemo「20 charts」补齐的美国侧指标：就业（初请/续请/非农同比）、新屋销售、Case-Shiller 房价、汽车销量、WEI 周经济指数、美 PPI/CPI 同比（配铜金比/油金比）、耐用品订单同比、高收益债信用利差（OAS，放在①利率区搭配 VIX），以及 ETF 区的 XLY/XLP 可选 vs 必选消费比价。

- **取数方式**：FRED 官方 `fredgraph.csv` 公开接口（**无需 API key**），逐序列取回后本地派生（4 周均值 / 同比）。首次加载约 15 秒（序列间 1.2s 限速），之后 **6 小时进程内 TTL 缓存**——不需要新的 launchd agent，`biao restart` 后首个访问者付一次冷启动。
- **降级**：单序列失败只进页面 `errors`，其余照常（与基本面参考层既有原则一致）。
- **FRED 限流**：连续请求会返回 404/挑战页，代码内已做「退避 4s 重试一次 + 序列间限速」；若整批失败，等几分钟再点「刷新」即可。
- **两个口径说明**：① ISM 制造业 PMI 因 ISM 授权已从 FRED 下架（NAPM 404），用耐用品订单同比替代「制造业订单&采购」视角；② 成屋销售受 NAR 授权 FRED 仅存近 13 个月，用 Census 新屋销售（1954 年起全史）。
- **分档阈值**：各卡区间为历史分布/事件锚点的经验归纳（详见前端 zones.ts 与抽屉脚注），非官方标准。
- **AAII / NAAIM 情绪**：会员制周度调查、无免费 API，读取端（`market_context/sentiment.py`）已就位——将周度 CSV 放入 `data/sentiment/` 即可在 workspace 侧点亮。

---

## 11. 因子观测台（/factors 数据源）

`/factors` 因子观测台依赖冻结快照 `factor_panel_snapshot.json`（落在 `LEI_CACHE_ROOT`），由以下命令生成：

```bash
python scripts/precompute_factor_panel.py
```

它扫描 K 线缓存（`*.bars.parquet`）里的全部标的（≥300 根），计算：已实现波动率及 3 年滚动分位、12-1 动量、20 日短动量（同组截面分位）、ADX14，并给板块做 12-1 动量排名。**不联网、只读缓存**，秒级完成。

### 11.1 因子评级是静态留痕（重要）
页面上的评级卡（实证稳健 / 板块层有效 / 反向因子 / 未通过检验）是 **2026-08-27 本地数据实证的静态留痕**，证据与检验脚本记录见 `docs/research-momentum-and-system-integration.md` 第六节。重跑预计算**不会**更新评级；只有重做实证研究后才改 `src/lei_signal/market_context/factor_panel.py` 里的 `FACTOR_META`。

### 11.2 故障排查
| 现象 | 处理 |
|---|---|
| `/factors` 打开提示「尚未运行预计算」 | 跑 `python scripts/precompute_factor_panel.py`；页面「刷新」按钮只重读磁盘，不重算 |
| 某标的显示「留痕中」 | 历史不足（RV 分位需 ≥252 日、12-1 需 ≥253 日），新上市/新缓存标的属正常 |
| 某标的显示「数据较旧」 | 该标的 K 线缓存 >14 天未更新，先排查日 K 维护任务 |
| ETF 行没有中文名 | 属正常：名称只用仓库权威源（板块用 `THS_INDUSTRY_NAMES`）；ETF 名称映射是待办（需接东财/腾讯名称接口） |

### 11.3 判定口径说明（给用户）
全部因子读数标 `research_proxy`：**不出买卖点、不挡任何 LEI 技术信号**。「高波·信号打折提示」「短线超买警示」是实证支持的风险标注（前者 6/8 年稳健、后者为反向因子），不是看空信号；ADX 未通过分年稳健性检验，仅留痕。ETF/指数的组内分位按 A股/美股/板块三组分别排名。

## 12. 资讯流 newsfeed（/news 页数据源）

基本面消息检索与排序系统：**每天 20:30** 自动抓取 → 粗筛入库 → LLM 批量打分（类别/重要性 0-10/利多利空/标的/一句话点评）→ 今日整合简报。数据在 lab.db（迁移 016），页面上也可点「立即刷新」手动触发。

- 数据源：东财 7x24 快讯、新浪 7x24（关键词粗筛后入库）、Google News 定制查询（如 NVDA earnings）、B站 UP主投稿（config/newsfeed.json 配置 mid 清单）、通用 RSS（预留公众号 wewe-rss，P2）。
- 配置：`config/newsfeed.json`（UP主 mid / gnews 查询 / 重点标的 / 粗筛关键词 / 回看天数）。
- 排序口径：已评分条目按重要性降序在前，未评分垫底按时间倒序；「博主观点」为独立类别 tab。
- LLM 复用监督员通道（DEEPSEEK_API_KEY > ARK_* > ANTHROPIC_*），失败自动降级为未评分态，不影响抓取。
- 自选联动（2026-09-03）：`GET /api/news/watchlist-brief?days=3` 聚合「我的自选 × 近 N 天消息」——每标的多空计数 + 最新一条 + 全局多空温度，供 /news 页顶部「自选消息雷达」；`/api/news/items` 新增 `symbol`（symbols 数组/标题/摘要匹配）与 `direction`（利多/利空专项排查）过滤参数，关键词搜索 `q` 现在也覆盖 symbols 列。仍是参考层：只做结构化对齐，不产生交易信号。

### 12.1 B站风控与字幕（重要）
| 现象 | 处理 |
|---|---|
| `bilibili:*` 报 `-352` / HTTP 412 风控页 | 属匿名态正常波动：管线已带 2s 限速 + cookie 持久化（`~/.lei_signal_lab/cache/newsfeed/bili_cookies.json`）+ 退避重试；偶发失败次日自愈，不阻断其他源 |
| B站视频没有字幕总结（只有标题+简介） | AI 字幕（ai_zh）需要登录态：浏览器 F12 → Cookies → 复制 `SESSDATA` → 写入 `.env` 的 `BILI_SESSDATA=`，重启后端即生效 |
| 想换/加 UP主 | 改 `config/newsfeed.json` 的 `bili_ups`（mid 从 UP主主页 URL `space.bilibili.com/<mid>` 取） |

### 12.2 故障排查
| 现象 | 处理 |
|---|---|
| 页面空白/「没有符合条件的条目」 | 手动跑 `python scripts/precompute_newsfeed.py --full` 看输出；或页面上点「立即刷新」 |
| 想全量回看历史 | `python scripts/precompute_newsfeed.py --full`（忽略水位，按 lookback_days 回抓） |
| 只想抓取不打分（省 token） | `python scripts/precompute_newsfeed.py --no-llm` |
| 简报没生成 | LLM 通道问题：查 `.env` 的 ARK_*/DEEPSEEK_API_KEY/ANTHROPIC_* 是否配置；打分/简报失败不影响条目入库 |
| 临时停用每日任务 | `launchctl bootout gui/$(id -u)/com.lei.newsfeed` |

### 12.3 公众号接入（P2 待办）
wewe-rss（docker 本地 4000 端口）把微信读书订阅的公众号转成 RSS，把生成的 feed 地址填进 `config/newsfeed.json` 的 `rss_feeds` 即可，管线已支持（category=blogger）。部署时需微信读书扫码，token 偶发失效重扫即可，失效期间不影响其他源。
