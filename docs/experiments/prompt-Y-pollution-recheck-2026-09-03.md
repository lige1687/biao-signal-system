# Prompt Y：遗留债务复核——模块B污染数据重算 + 中证500重审（债务一+债务三）

你在 `biao-signal-system` 仓库工作（当前分支 `recovery/lei-round2`）。只读研究
任务：**不改 `src/` 下任何生产代码，不 push，不删任何现有文件，不改任何现有
归档**，产出全部为新增文件。

## 开工前同步（简版，几分钟内完成）

1. `git fetch --all`，确认本地 `recovery/lei-round2` 与远端一致（本任务书发出时
   最新提交为 356f005；若远端有更新，先 pull 再浏览新增归档，避免与在途任务
   重复）。pull 若出现冲突，停下来在报告里如实描述，不要用 `--force` 或丢弃
   本地更改的方式强行解决。
2. 通读 `docs/next-steps-master-plan-2026-09-02.md` 第二部分（遗留技术债务）
   的债务一、债务三两节——本任务就是执行这两条。

## 任务背景

数据质量审计（`docs/experiments/data-quality-jump-audit-2026-09-02.md`）发现
A 股 ETF 池有 13 处复权断裂（就是价格历史里凭空出现的假跳变，会污染回测
结果），其中中证1000ETF(512100) 的数据是被污染的重灾区。由此留下两笔账：

1. **债务一（模块B）**：`docs/experiments/broad-index-portfolio-and-signals-ARCHIVE-2026-09-02.md`
   第 4.1 节已标注——模块B（密集突破）在宽基/行业 ETF 池上唯一算出的正收益，
   45% 来自被污染的中证1000ETF 数据。这个正收益是否还成立，没人重算过。
2. **债务三（中证500）**：同一份审计发现，中证500(510500) 此前"不适合这套
   规则"的判负结论建立在被污染的数据上——排除假数据后从"明显不适合"变成
   "基本打平"，审计建议"需要重审，不直接下入选结论"。这个重审还没人做。

## 任务步骤

### 第一步：重建修平口径（不重新发明）

读 `data-quality-jump-audit-2026-09-02.md`，拿到 13 处断裂的完整清单和该报告
使用的临时修平方法（排除异常点）。本任务**复用**这个方法，不重新设计修平
逻辑；如果发现修平方法本身有缺陷，如实写进报告留给用户决定，不要自行发明
新方法硬算。

### 第二步：债务一——模块B重算（判定标准跑前写死）

1. 找到模块B在宽基/行业 ETF 池上那次回测的原始脚本和参数（归档报告里有
   记录），**原参数原框架重跑**，只把数据换成修平后的版本。
2. 跑之前先在脚本 docstring 里写死判定标准，例如（具体数值由你依据原报告
   的量级论证后预注册，不得跑完再调）：
   - "45% 正收益仍成立"的条件：修复后模块B整体期望收益仍为正，且排除
     中证1000 后其余标的的贡献不足以推翻"模块B在池子上有效"的结论；
   - "不成立"的条件：修复后整体期望转负，或正收益完全由污染段贡献。
3. 双跑哈希（PYTHONHASHSEED=0/42 两次全量运行结果须一致，记录哈希值）。

### 第三步：债务三——中证500重审（判定标准跑前写死）

1. 先找到中证500当初判负的那份归档（可能在 `full-pool-revalidation-ARCHIVE-2026-09-02.md`
   或 `docs/timing-sweep/` 终审表里），重建它当时用的判定口径。如果当初
   没有明确写判定标准，先从报告数字反推一个口径并写进 docstring，注明
   "这是事后重建的口径"。
2. 用修平后的干净数据，**原规则原参数**重跑（不重新调参、不做参数挑选——
   这是复核不是新实验）。
3. 预注册三选一判定："维持判负" / "翻案为值得进一步验证" / "证据不足"，
   写清楚各自需要的数字条件。注意审计只说了"基本打平"，**打平不等于值得
   入选**，翻案的门槛要明显高于打平。
4. 双跑哈希，要求同上。

### 第四步：归档

产出 `docs/experiments/pollution-recheck-moduleB-csi500-2026-09-XX.md`（一份
报告两大节，模块B一节、中证500一节），包含：修平口径引用、预注册判定标准
全文、两次运行哈希、结果数字、判定结论、对用户实际意味着什么。

## 红线（逐条遵守，与 AGENTS.md 一致）

- 不使用"买入/卖出/建议买/该买/加仓/减仓/抄底"等买卖指令类词汇；不给
  "该不该动"的综合建议——只呈现复核数字和判定。
- 报告开头必须有一段大白话（用户不是量化背景）：这两笔账是什么、重算后
  结果各是什么、对用户意味着什么。技术词第一次出现必须给一句解释。
- 判定标准跑前写死，跑完不得回头调整；调整了宁可如实报告"预注册标准
  设计不当"重跑，也不要悄悄改线。
- 中证500 即使翻案也只是"值得进一步验证"，不是入选结论——审计报告原文
  就这么要求的，不要越权下结论。
